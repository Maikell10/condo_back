ON DELETE CASCADE vs SET NULL: Si eliminas un edificio, sus apartamentos y recibos se borran en cascada para no dejar basura. Pero si eliminas un usuario (propietario), el apartamento no se borra, simplemente su campo owner_id queda en NULL para que puedas asignarle un dueño nuevo.

status en Pagos: Fíjate que por defecto un pago entra como PENDING_APPROVAL. Así, cuando el administrador lo revise en su panel y confirme que el dinero cayó en el banco, lo pasa a APPROVED.


git add .
git commit -m "update"
git push -u origin main


https://vercel.com/maikellods10s-projects/condo-front
https://condo-front-seven.vercel.app/
https://condoback.vercel.app/


800 para correo

Gmail (Cuenta personal): Es la alternativa directa más común. Permite enviar hasta 500 correos al día de forma gratuita. Con este límite diario, podrías cubrir tus 800 correos mensuales en poco más de día y medio. Para usarlo con Nodemailer, solo requieres configurar una Contraseña de Aplicación de Google en tu cuenta.

Encuestas Comunidad : para llevar a cabo temas a aprobar por la comunidad

-en vista de propietario se coloca solo num de apto para morosos o pendientes


-En admin agregar el monto a cobrar a cada edificio

-agregar campo libre q se vaya guardando en gastos

-Agregar tipo conjunto de edificio en donde un administrador de edificio puede llevar n edificios y puede cargar gastos induviduales a cada uno fuera de los comunes




------------------
ALTER TABLE payments 
ADD COLUMN currency ENUM('USD', 'VES') DEFAULT 'VES' AFTER amount,
ADD COLUMN exchange_rate DECIMAL(15,4) DEFAULT 1.0000 AFTER currency,
ADD COLUMN amount_local DECIMAL(15,2) NULL AFTER exchange_rate,
ADD COLUMN payment_method VARCHAR(50) NULL AFTER amount_local;


<div class="text-right">
    <div class="text-xs uppercase font-bold text-gray-400">Total a Pagar</div>
    <div class="text-3xl font-black text-indigo-600">${{ data.totalBill | number:'1.2-2' }}</div>
    
    <!-- Equivalencia en Bs -->
    <div class="text-sm font-bold text-gray-500 mt-1">
        Ref: Bs. {{ (data.totalBill * tasaDelDia) | number:'1.2-2' }}
    </div>
</div>


{
  "apartmentId": 41,
  "receiptId": 15,
  "currency": "VES",
  "amountLocal": 1600,
  "exchangeRate": 40.00,
  "reference": "123456789",
  "paymentMethod": "PAGO_MOVIL"
}


// En Node.js, calculas el amount (USD) real que entró basado en la tasa:
const amountInUsd = req.body.currency === 'VES' 
    ? (req.body.amountLocal / req.body.exchangeRate).toFixed(2) 
    : req.body.amountLocal;

// Y haces el INSERT guardando toda la historia:
// amount: amountInUsd (ej. 40.00)
// currency: 'VES'
// exchange_rate: 40.00
// amount_local: 1600.00